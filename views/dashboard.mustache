{{> header}}
<div class="container mt-4">
  <h1>Dashboard</h1>
  <p>Welcome, {{user.username}}! This is your personal dashboard where you can view and manage your goals and achievements. On the left side, you'll see your current goals, and on the right side, your achievements are listed. To manage your goals and achievements, click the corresponding buttons below each section.</p>
  <div class="row">
    <div class="col-md-6">
      <h2><img src="/images/Fitness.jpg" alt="Fitness" class="img-fluid" style="width: 40px; height: 40px; margin-right: 10px;">You have {{activeGoalsCount}} Active Goals</h2>
      <div class="text-center mb-2">
        <a href="/goals" class="btn btn-primary">Manage Goals</a>
      </div>
      <ul class="list-group mb-3">
        {{#goals}}
          {{^completed}}
            <li class="list-group-item">{{category}}: {{description}}</li>
          {{/completed}}
        {{/goals}}
      </ul>
    </div>
    <div class="col-md-6">
      <h2><img src="/images/Nutrition.jpg" alt="Nutrition" class="img-fluid" style="width: 40px; height: 40px; margin-right: 10px;">You have {{achievements.length}} Achievements</h2>
      <div class="text-center mb-2">
        <a href="/achievements" class="btn btn-primary">Manage Achievements</a>
      </div>
      <ul class="list-group mb-3">
        {{#achievements}}
          <li class="list-group-item">{{description}}</li>
        {{/achievements}}
      </ul>
    </div>
  </div>
</div>
{{> footer}}
